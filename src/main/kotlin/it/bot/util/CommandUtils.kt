package it.bot.util

object CommandUtils {

    /*fun matches(botCommand: String, text: String): Boolean {
        return text.startsWith("$botCommand ", true) or
                (text.equals(botCommand, true)) or
                text.startsWith("$botCommand@$botUsername ", true) or
                (text.equals("$botCommand@$botUsername", true))
    }

    fun isExactMatch(botCommand: String, text: String): Boolean {
        return (text.equals(botCommand, true)) or
                (text.equals("$botCommand@$botUsername", true))
    }*/
}